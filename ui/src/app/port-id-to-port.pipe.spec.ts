import {PortIdToPortPipe} from './port-id-to-port.pipe';

describe('PortIdToPortPipe', () => {
  it('create an instance', () => {
    const pipe = new PortIdToPortPipe();
    expect(pipe).toBeTruthy();
  });
});
